@model Core.Domain.Employee 
@{
    ViewData["Title"] = "Create Employee";
}

<h2>Create Employee</h2>

@using (Html.BeginForm())
{
    <div class="form-group">
        @Html.EditorForModel()
    </div>

    <p>
        <input type="submit" class="btn btn-default" value="Create Employee" />
    </p>
}